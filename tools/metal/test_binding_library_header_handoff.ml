let ()=if Array.length Sys.argv<>2 then invalid_arg"inventory"else let open Yojson.Safe.Util in let expected=Binding_library42_native_closure.existing_ids@Binding_library42_native_closure.remaining_ids in let items=Yojson.Safe.from_file Sys.argv.(1)|>member"symbols"|>to_list|>List.filter_map(fun j->let id=j|>member"id"|>to_string in if List.mem id expected then Some(Binding_library_header_handoff.classify~kind:(j|>member"kind"|>to_string)id)else None)in Binding_library_header_handoff.validate items;print_endline"MTLLibrary42: exact closure across current classifications"
