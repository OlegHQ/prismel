let ()=let module C=Binding_metal4_integration_coverage in if List.length C.compute_ownership_ids<>32 then failwith"compute ownership count";if List.length(List.filter(fun id->List.mem id C.all_ownership_ids)C.covered_ownership_ids)<>38 then failwith"covered ownership count";if List.length C.remaining_ownership_ids<>113 then failwith"remaining ownership count";let open Binding_metal4_compute_validation in let r={device=1;live=true;length=64}in assert(pair~device:1~source:r~source_offset:0~destination:r~destination_offset:4~size:8~alignment:4=Ok());match pair~device:2~source:r~source_offset:0~destination:r~destination_offset:4~size:8~alignment:4 with Error _->()|Ok()->failwith"cross-device accepted"
