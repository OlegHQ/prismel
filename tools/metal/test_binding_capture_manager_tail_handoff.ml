let ()=if Array.length Sys.argv<>2 then invalid_arg"inventory"else let open Yojson.Safe.Util in let expected=Binding_capture_manager_tail_handoff.callable_ids in let items=Yojson.Safe.from_file Sys.argv.(1)|>member"symbols"|>to_list|>List.filter_map(fun j->let id=j|>member"id"|>to_string and kind=j|>member"kind"|>to_string and c=j|>member"classification"|>to_string in if (List.mem id expected||id="class:MTLCaptureDescriptor"||id="class:MTLCaptureManager")&&(c="unreviewed"||c="bound")then Some(Binding_capture_manager_tail_handoff.classify~kind id)else None)in Binding_capture_manager_tail_handoff.validate items;print_endline"CaptureManager21: exact callable19 + metadata2 closure"
