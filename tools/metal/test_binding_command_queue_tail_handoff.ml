let ()=if Array.length Sys.argv<>2 then invalid_arg"inventory"else let open Yojson.Safe.Util in let expected="class:MTLCommandQueueDescriptor"::Binding_command_queue_tail_handoff.callable_ids in let rows=Yojson.Safe.from_file Sys.argv.(1)|>member"symbols"|>to_list|>List.filter(fun j->List.mem(j|>member"id"|>to_string)expected)in let items=List.map(fun j->Binding_command_queue_tail_handoff.classify~kind:(j|>member"kind"|>to_string)(j|>member"id"|>to_string))rows in Binding_command_queue_tail_handoff.validate items;let status id=rows|>List.find(fun j->j|>member"id"|>to_string=id)|>member"classification"|>to_string in if List.exists(fun id->status id<>"bound")Binding_command_queue_tail_handoff.callable_ids then failwith"CommandQueue14 promotion drift";if status"class:MTLCommandQueueDescriptor"<>"bound"then failwith"CommandQueue metadata drift";print_endline"CommandQueue15: exact15 bound"
