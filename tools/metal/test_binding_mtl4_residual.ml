let ()=let open Binding_mtl4_safe_graph in let b=command_buffer 1 in let e=match encoder b with Ok x->x|Error x->failwith x in(match use_resource e{device=2;id=1}with Error _->()|Ok()->failwith"cross-device accepted");assert(use_resource e{device=1;id=2}=Ok());assert(finish e=Ok());(match finish e with Error _->()|Ok()->failwith"double end");assert(commit b=Ok());assert(complete b=Ok());assert(b.retained=0)
