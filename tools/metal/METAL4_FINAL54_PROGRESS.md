# Metal4 final-54 progress

The first coherent stage closes **19/54** remaining ownership IDs through the synchronous
machine-learning descriptor/compiler path: all ten descriptor method/property
IDs, the compiler creation selector, six ownership-bearing state method/property IDs, and both
reflection-binding IDs plus their owned materialization path. Exact tensor
extents use bounded `int64 array` values; returned library, pipeline, and
reflection data are owned or copied.

The remaining **35** split into archive/pipeline descriptor graphs, render-pass
attachment graphs, specialization/stitched-function graphs, queue mapping and
feedback, and asynchronous compiler selectors. Async selectors require the
existing bounded compiler-task completion registry rather than unrooted OCaml
callbacks.
