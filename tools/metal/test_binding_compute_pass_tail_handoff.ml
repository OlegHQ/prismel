let ()=if Array.length Sys.argv<>2 then invalid_arg"inventory"else let open Yojson.Safe.Util in let rows=Yojson.Safe.from_file Sys.argv.(1)|>member"symbols"|>to_list|>List.filter(fun j->j|>member"header"|>to_string="Metal/MTLComputePass.h")in let items=List.map(fun j->Binding_compute_pass_tail_handoff.classify~kind:(j|>member"kind"|>to_string)(j|>member"id"|>to_string))rows in Binding_compute_pass_tail_handoff.validate items;let status id=rows|>List.find(fun j->j|>member"id"|>to_string=id)|>member"classification"|>to_string in if List.exists(fun id->status id<>"bound")Binding_compute_pass_tail_handoff.callable_ids then failwith"ComputePass17 promotion drift";let metadata=List.filter(fun(item:Binding_compute_pass_tail_handoff.item)->item.lane=Binding_compute_pass_tail_handoff.Metadata)items in if List.exists(fun(item:Binding_compute_pass_tail_handoff.item)->status item.id<>"unreviewed")metadata then failwith"ComputePass metadata drift";print_endline"ComputePass20: callable17 bound + metadata3 unreviewed; packages3/6/3/3/2/3"
