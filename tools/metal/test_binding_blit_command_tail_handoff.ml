let ()=if Array.length Sys.argv<>2 then invalid_arg"inventory"else let open Yojson.Safe.Util in let expected=Binding_blit_command_tail_handoff.callable_ids in let items=Yojson.Safe.from_file Sys.argv.(1)|>member"symbols"|>to_list|>List.filter_map(fun j->let id=j|>member"id"|>to_string in if List.mem id expected then Some(Binding_blit_command_tail_handoff.classify id)else None)in Binding_blit_command_tail_handoff.validate items;print_endline"BlitCommand25: exact callable closure across current classifications"
