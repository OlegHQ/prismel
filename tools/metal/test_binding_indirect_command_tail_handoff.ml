let ()=if Array.length Sys.argv<>2 then invalid_arg"inventory"else let open Yojson.Safe.Util in let rows=Yojson.Safe.from_file Sys.argv.(1)|>member"symbols"|>to_list in let items=List.map(fun id->let j=List.find(fun j->j|>member"id"|>to_string=id)rows in if j|>member"classification"|>to_string<>"bound"then failwith("IndirectCommand ID is not bound: "^id);Binding_indirect_command_tail_handoff.classify~kind:(j|>member"kind"|>to_string)id)Binding_indirect_command14_safe_package.ids in Binding_indirect_command_tail_handoff.validate items;print_endline"IndirectCommand14: exact14 bound"
