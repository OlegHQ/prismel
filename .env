# Safe defaults for display-less Linux development and CI.
#
# Runtime selects and configures SDL's dummy video/audio drivers plus the
# software renderer at its backend boundary. Use "native" or "web" to switch
# targets without changing application code.
PRISMEL_RENDER_TARGET=native

